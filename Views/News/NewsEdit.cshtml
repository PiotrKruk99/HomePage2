@{
    ViewData["Title"] = "Dodawanie i edycja wiadomości";
}

<h2>edycja wiadomości</h2>
<form><button type="submit" asp-controller="News" asp-action="AddArticleGet">Dodaj...</button></form>

@foreach (Article item in ViewBag.news)
{
    <p>
        <b>@item.Title</b><br>
        @Html.Raw(@item.Content)<br>
        <form method="post" asp-controller="News" asp-action="DeleteArticle">
            <button type="submit" name="deleteId" value="@item.Id">Usuń...</button>
        </form>
        <form method="post" asp-controller="News" asp-action="EditArticle">
            <button type="submit" name="editId" value="@item.Id">Edytuj...</button>
        </form>
    </p>
}